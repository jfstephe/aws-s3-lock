{
  "name": "aws-s3-lock",
  "version": "1.0.1",
  "description": "Creates a lock in S3 without the need for a database",
  "main": "build/index.js",
  "scripts": {
    "prepare": "rm -rf ./build && tsc",
    "build": "rm -rf ./build && tsc",
    "clean": "rm -rf ./node_modules && rm package-lock.json",
    "test": "mocha"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jfstephe/aws-s3-lock.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jfstephe/aws-s3-lock/issues"
  },
  "homepage": "https://github.com/jfstephe/aws-s3-lock#readme",
  "devDependencies": {
    "@types/mocha": "^9.1.0",
    "@types/node": "^17.0.23",
    "@types/sinon": "^10.0.11",
    "mocha": "^9.2.2",
    "sinon": "^13.0.1",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "aws-sdk": "^2.1110.0"
  }
}
